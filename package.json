{
  "name": "auto-translate-app",
  "version": "1.0.0",
  "description": "Wonderful automation",
  "author": "dkyoo <dkyoo@linecorp.com>",
  "license": "ISC",
  "repository": "https://github.com/dkstyle/auto-translate-app.git",
  "homepage": "https://github.com/dkstyle/auto-translate-app",
  "bugs": "https://github.com/dkstyle/auto-translate-app/issues",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.2",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
